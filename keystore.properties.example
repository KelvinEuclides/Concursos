# Copy to `keystore.properties` (git-ignored) for signed local release builds.
# CI reads the same keys from GitHub Actions secrets instead.

KEYSTORE_FILE=keystore/release.jks
KEYSTORE_PASSWORD=changeit
KEY_ALIAS=concursos
KEY_PASSWORD=changeit
